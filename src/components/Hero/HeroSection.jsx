import Carousel from './Carousel.jsx';

export default function HeroSection() {
    return(
        <section className="hero-section" style={{marginTop: '3%'}}>
            <Carousel />
        </section>
    );
}